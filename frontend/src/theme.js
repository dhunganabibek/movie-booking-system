import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0052cc",
    },
    secondary: {
      main: "#edf2ff",
    },
    navbarcolor: {
      main: "#933553",
    },
    white: {
      main: "#FFF",
    },
    buttoncolor: {
      main: "#9457eb",
    },
  },
});

export default theme;
